<h2>{{model.title}}</h2>
<tbody>
  <tr>
    <th>No</th>
    <th>Title</th>
    <th>Length</th>
  </tr>
  {{#each track in model.tracks}}
  <tr>
    <td>{{track.position}}</td>
    <td>{{track.name}}</td>
    <td>{{track.duration}}</td>
  </tr>
  {{/each}}
</tbody>
